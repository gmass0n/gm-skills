interface:
  display_name: "Jira Discovery"
  short_description: "Interview, discover, and publish a Jira brief."
  default_prompt: "Use $jira:discovery to interview me, investigate this Jira task, and publish its implementation brief."
