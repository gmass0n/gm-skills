interface:
  display_name: "Jira Discovery"
  short_description: "Publish an evidence-backed Jira brief."
  default_prompt: "Use $jira:discovery to investigate this Jira task and publish its implementation brief."
